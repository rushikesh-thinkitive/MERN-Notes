/*

1) A cookie is a small piece of text data set by web server that resides on the client machine
2) once it set's, so it will automatically send by the client machine with each request
3) web server have access while sending each and every response
4) Only that web server can access cookie data which have save on client machine, other web server can't access
5) There are 2 types of cookie :
    1.Session Cookies - Cookies that are set without expires field are called session cookies.
                        It will destroy when user quit the browser

    2. Persistent Cookies - The browser keeps it up until there expiration date is reached
    
*/
