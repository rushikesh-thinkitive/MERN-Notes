/*

1) app.param()  -  function is used to add the callback triggers to route parameters 
   It is commonly used to check data exist for which we request.

2) It will call only once when particular route hits 
3) for each route it only call at once

*/